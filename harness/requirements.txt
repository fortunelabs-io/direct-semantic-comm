# Harness v1 host dependencies, fully pinned including transitives.
#
# Regenerate with `mise run freeze` after a deliberate upgrade, never as a side
# effect of installing something. A capture taken under one resolution and a
# capture taken under another are two measurements, and the difference between
# them belongs in the record rather than in a lockfile nobody read.
#
# Direct: pyserial, numpy, pandas, matplotlib, scipy.
# Everything else below is transitive and is pinned for the same reason.
#
# Resolved against Python 3.12.13, the version pinned in .mise.toml.

contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.11.1
numpy==2.5.2
packaging==26.3
pandas==3.0.5
pillow==12.3.0
pyparsing==3.3.2
pyserial==3.5
python-dateutil==2.9.0.post0
scipy==1.18.0
six==1.17.0
